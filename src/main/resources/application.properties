spring.application.name=demaker

# JWT Configuration
app.jwt-secret=demaker-super-geheime-jwt-sleutel-voor-token-signing-minimaal-32-karakters
app.jwt-expiration=86400000

# Database configuratie
spring.datasource.url=jdbc:postgresql://localhost:5433/autogarage_db
spring.datasource.username=postgres
spring.datasource.password=Tijdomteleren22.

# JPA/Hibernate (later omzetten naar update voor nu automatisch tabellen maken)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

## Security tijdelijk uit
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Data initialization
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=15MB